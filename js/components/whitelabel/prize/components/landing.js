'use strict';

import React from 'react';

let Landing = React.createClass({
    render() {
        return (
            <div>
                Hello from landing
            </div>
        );
    }
});

export default Landing;
