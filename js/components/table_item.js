import React from 'react';

let TableItem = React.createClass({
    render() {
        return (
            <ul>
                <TableItem />
            </ul>
        );
    }
});

export default TableItem;